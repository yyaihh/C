#include<stdio.h>
#include<stdlib.h>
void main() {
	int n;
	printf("请输入0到n^2的自然数构成的魔方阵\n");
	sacnf("%d", &n);
	int* arr = (int*)malloc(n * n * sizeof(int));
	for (int i = 0; i < 2 * n + 2; ++i) {
		
	}
}